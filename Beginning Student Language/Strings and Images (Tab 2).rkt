;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |Strings and Images (Tab 2)|) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
(require 2htdp/image)

;(circle 10 "solid" "red")
;(rectangle 30 60 "outline" "blue")
;(text "hello" 24 "orange")

;Other are "above" "beside"
;(overlay (circle 10 "solid" "red")
;       (circle 20 "solid" "yellow")
;       (circle 30 "solid" "green"))

; In Video Quizzes
; Which primitive is required in the following expression to produce the stop sign image below
; overlay

; Which image will the following expression produce
; 3

; Why are we getting the following error
; 2, We didn't include (require 2htdp/image) at the beginning of the program

; Why are we still getting an error
; 3, We misspelled the name of the module, so we need to replace...