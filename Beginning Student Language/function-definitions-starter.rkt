;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname function-definitions-starter) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
(require 2htdp/image)

;; function-definitions-starter.rkt

(define (bulb c)
  (circle 40 "solid" c))

;(above (bulb "red")         
;       (bulb "yellow")
;       (bulb "green"))

; left to right, inside out
;(bulb (string-append "re" "d"))

; In Video Quizzes
; Why should you use function definition?
; All of the above

; What function would define a function to calculate pythag
; 1

; What should we do to fix the following error
; 3, replace i with img.

; Given the function
;(define (foo a b)
;  (+ (* 3 a)
;     b
;     (* b a)))
; replace foo with its definition, replaceing all instances of a with 2 and b with 4

; After the previous... next step?
; 3

; next step?
; 2

; next step?
; 3
